The project is composed in two parts the frontend and backend each of which are in seperate folders

Frontend
- the frontend is written in TypeScript with React Native using expo and gluestack

Backend
- The backend is writen in TypeScript with Node.js, Express and Mongoose
- The backend connects to MongoDB
